{ print "pgm 1: " $0 }                 # prints entire line
/Pen/ { print "pgm 2: " $1 " " $2 }    # prints line which matches with "Pen" word
$3 >= 5 && /[0-9]/ { print "pgm 3: " $1 " " $2 } # prints qty greater than or equal to 5
